package ru.dvfu.mrcpk.alekseenko.theme4;

import java.util.Scanner;

// 11.	Ввести с клавиатуры пятизначное число и определить, является ли оно палиндромом
public class Main11 {
           public static void main(String[] args) {
               Scanner in = new Scanner(System.in);
               System.out.print("Введите пятизначное число:");

            // Вводимое значение
             int x = in.nextInt();
             int a = (x / 10000);
               int b = (x / 1000);
               int c = (x / 10);
               double d = (x  );


              if (y == 0)
               {
                   System.out.print("Число" + " " + x + " " + "является палиндромом");
               }  else if (y != 0)
               {
               System.out.print("Число" + " " + x + " " + "не является палиндромом");
               }




}
}
